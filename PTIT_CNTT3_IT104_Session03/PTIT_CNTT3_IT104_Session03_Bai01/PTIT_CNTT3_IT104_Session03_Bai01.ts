const usetname: string = "Linh";
const age: number = 20;
const job: string = "Student";

const printInfo = (name: string, age: number, job: string): void => {
    console.log(`Name: ${name}, Age: ${age}, Job: ${job}`);
};
printInfo(usetname, age, job);